<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        LUMINA — Offline-first AI companion for crisis-affected children
        Or4cl3 AI Solutions | CHATRON 9.0

        Permissions policy:
        - INTERNET is declared for opportunistic NGO sync only
        - No location, no contacts, no microphone (Phase 1)
        - No advertising IDs, no analytics frameworks
    -->

    <!-- Opportunistic sync when NGO WiFi detected. Core functionality works without this. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Phase 2: voice input for pre-literate children -->
    <!-- <uses-permission android:name="android.permission.RECORD_AUDIO" /> -->

    <application
        android:name=".LuminaApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="LUMINA"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lumina"
        android:usesCleartextTraffic="false">

        <!-- Main activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Lumina.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
